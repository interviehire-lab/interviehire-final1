import './reschedule-reset.css';

export const metadata = {
  title: 'Reschedule your interview · IntervieHire',
  description: 'Pick a new time for your IntervieHire interview.',
};

export default function RescheduleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
