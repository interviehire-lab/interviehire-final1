'use client';

// Apply Link stage. Content is rendered by the persistent vanilla-JS surface;
// DashboardShell's navigateToPath() reads this route's slug and activates the
// matching job-detail tab. See src/dashboard/job-stages.js.
export default function ApplyLinkPage() {
  return null;
}
