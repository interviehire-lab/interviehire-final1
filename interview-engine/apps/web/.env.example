# interview-engine candidate room (apps/web)

# Backend (interview-engine API / Fastify)
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000/ws

# Vapi Web SDK for jobs with conversationalInterview enabled. The public key is
# designed for browser use; keep Vapi private keys and webhook secrets server-side.
NEXT_PUBLIC_VAPI_PUBLIC_KEY=
NEXT_PUBLIC_VAPI_ASSISTANT_ID=

# Privacy Policy the candidate links to from the informed-consent gate. Optional
# (falls back to https://www.interviehire.com/privacy). Point at wherever the
# policy is actually hosted for this deployment.
NEXT_PUBLIC_PRIVACY_URL=https://www.interviehire.com/privacy

# Pixel-streamed MetaHuman + Convai avatar for the candidate proctoring room
# (/interview). The UE5 app runs on the host PC's GPU and is exposed via an Epic
# Pixel Streaming signalling server behind a Cloudflare quick tunnel — paste that
# https://*.trycloudflare.com URL here (see ../../PIXEL_STREAMING_SETUP.md).
# Optional: the tunnel URL changes each restart, so the dashboard "Run test
# interview" launcher can override this per-run with a ?avatar=<url> param.
NEXT_PUBLIC_AVATAR_URL=

# Convai voice/avatar layer for the /interview/convai room (browser-native).
# Create a character at https://convai.com, then paste its API key + character ID.
# NOTE: NEXT_PUBLIC_* ships to the browser — use a key scoped/limited to this
# character, or proxy it server-side before going to production.
NEXT_PUBLIC_CONVAI_API_KEY=
# The MetaHuman interviewer character from the UE build (reuse or create a new one):
NEXT_PUBLIC_CONVAI_CHARACTER_ID=fac06dea-682c-11f1-98eb-42010a7be02e

# PRODUCTION PATH (per the team's verified plan): do NOT ship the API key to the
# browser. Instead have FastAPI mint a short-lived token via POST /user/connect
# and set per-candidate persona via POST /character/update {charID, backstory}.
# The NEXT_PUBLIC_* key above is for local dev of this room only.
